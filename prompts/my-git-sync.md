Follow the `git-sync` skill for this repository: stage all changes, commit only when there is something staged, then push the current branch. Use the skill as the source of truth for safety rules, commit message format, upstream handling, and reporting.
