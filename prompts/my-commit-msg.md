Review the work since the last commit (use `git status` and `git diff` to see what changed) and produce one short, descriptive commit message appropriate for the changes. Do not run `git commit` or otherwise create the commit. Display the suggested message only, so the user can use it themselves. If no diff is available, say what is missing and suggest a message based on described changes only.
